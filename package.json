{
  "name": "my-aws-app",
  "version": "1.0.0",
  "description": "AWS ECS Application with CI/CD",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'No build required'",
    "test": "jest --coverage --passWithNoTests",
    "lint": "echo \"No linting setup yet - skipping\" && exit 0"

  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": ["src/**/*.js"]
  }
}
